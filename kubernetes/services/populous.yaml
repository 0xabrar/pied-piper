apiVersion: v1
kind: Service
metadata:
  name: populous 
  labels:
    name: populous 
spec:
  ports:
  - port: 50052 
    protocol: TCP
    targetPort: 50052 
  selector:
    name: populous 
  type: ClusterIP

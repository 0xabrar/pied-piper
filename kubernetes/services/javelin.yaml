apiVersion: v1
kind: Service
metadata:
  name: javelin 
  labels:
    name: javelin 
spec:
  ports:
  - port: 80 
    protocol: TCP
    targetPort: 8080 
  selector:
    name: javelin 
  type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: javelin 
  labels:
    name: javelin 
spec:
  ports:
  - port: 50053 
    protocol: TCP
    targetPort: 50053 
  selector:
    name: javelin 
  type: ClusterIP

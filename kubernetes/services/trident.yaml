apiVersion: v1
kind: Service
metadata:
  name: trident 
  labels:
    name: trident 
spec:
  ports:
  - port: 80 
    protocol: TCP
    targetPort: 8080 
  selector:
    name: trident 
  type: ClusterIP
